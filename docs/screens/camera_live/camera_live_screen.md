# CameraLiveScreen

- **Dart file:** `lib/screens/camera_live/camera_live_screen.dart`
- **Route:** `/dashboard/live/:cameraId` (nested as a child route of the Dashboard branch inside the bottom-nav `StatefulShellRoute`, so the bottom nav bar stays visible here and throughout the whole Camera Settings sub-tree; see [navigation_flow.md](../navigation_flow.md))
- **Purpose:** Show a single camera's live feed with playback-style controls, and a secondary "Playback" tab for reviewing recorded footage. **Live tab video is real** for any camera with a saved LAN connection: `LiveViewController` (`lib/app_state/live_view_controller.dart`) implements `packages/camera_api/STREAMING_GUIDE.md`'s LAN WebRTC path end to end — `GetLiveStreamUri` discovery (`LiveStreamUriClient`, replacing the retired `GetWebRtcUri`/`WebRtcUriClient` 2026-09-11 — the camera now decides LAN transport itself, WebRTC or RTSP, per profile), offer/answer signaling over `package:flutter_webrtc`, no STUN/TURN (host candidates only, same-LAN), a grace window before an ICE `disconnected` blip is treated as a real drop, fresh-offer reconnect on a genuine drop, and a best-effort `POST /webrtc/stop` on teardown. **Real bug fix, 2026-09-11**: live view now always requests `kMobileOnlyStreamProfileToken` (`Profile_3`, the dedicated mobile stream) when no explicit profile is given — it used to default to `Profile_1` (the NVR/VMS-facing main stream), a real bug flagged in the updated `STREAMING_GUIDE.md`. **RTSP LAN fallback, real (2026-09-11)**: when the camera reports `LiveStreamTransport.rtsp` for this profile (its firmware build has `WEBRTC_STREAMING` disabled — the documented current default), `LiveViewController._connectRtsp` opens an `RtspLiveViewSession` (`lib/rtsp/rtsp_live_view_session.dart`, adapted from the Playback tab's `RtspReplaySession` — same DESCRIBE/SETUP/PLAY/digest-auth/H.264+AAC depacketization, but no seek/`Range:` concept) behind an `RtspLiveViewProxy` (`lib/rtsp/rtsp_live_view_proxy.dart`, adapted from `RtspRemuxProxy`) that remuxes to fMP4 over a local HTTP loopback for a plain `video_player` surface (`LiveViewController.lanRtspVideoController`) — still a same-network ("LAN") session, no "Remote" badge, same mute/health-monitor/reconnect treatment as the WebRTC path. This is a plain `video_player`, not a WebRTC `MediaStreamTrack`, so Talk and Record are unavailable on it (guarded in the UI with "isn't available on this connection", same message WAN already used) and Snapshot falls back to `_captureFrame`'s existing `RenderRepaintBoundary` screenshot path (the same fallback already used when there's no WebRTC track at all) rather than `MediaStreamTrack.captureFrame()`. The Live tab's default session also now pins `kMobileOnlyStreamProfileToken` permanently (`_profileLadderEnabled = false` unless an explicit `initialProfile` is passed, e.g. Multiview) so a healthy connection can never auto-step up to `Profile_1`/`Profile_2`, per `STREAMING_GUIDE.md`'s "never fall through, under any circumstance" rule. WAN fallback (KVS) is wired up here too — see below. The Live tab, for any camera with **no** saved connection, shows `_LiveUnavailable` ("Camera not connected") instead of any video — removed the previous bundled dummy sample clip fallback entirely 2026-09-07 per direct user request ("remove the dummy video in the app completely") rather than keep showing fake footage; in practice this state is rare since every onboarding path sets a connection and `isOnline` together, so `_OfflineThumbnail`/`_OfflineOverlay` (below) normally cover a disconnected camera first. **The Playback tab is real recordings, streamed live over RTSP, not the dummy asset and not a download-then-play temp file** (2026-09-07, `RecordingsClient` for the clip list + `OnvifReplayControlClient`/`lib/rtsp/` for the actual video — rebuilt entirely from the original `RecordingsClient.downloadClip`-based design) — see LIVE-003/LIVE-012/LIVE-036's rows below; a camera with no saved connection, or nothing recorded for the selected day, shows LIVE-051 ("No recording available") instead of any placeholder video. **The scrubber (LIVE-012) is `ScrollableRecordingTimeline`** (`lib/widgets/scrollable_recording_timeline.dart`, replacing the earlier non-scrolling `RecordingMapTimeline` 2026-09-07 per direct user request — "if 24 hour video is there how will user interact with that, the current bar is too small": a whole day's footage squeezed into one fixed-width bar gives too little precision per pixel to tap/drag accurately on a dense/continuous-recording day). This is a **new** widget (not a revert to the shared `CameraTimeline` [events_screen.md](../events/events_screen.md) still uses, and not `CameraTimeline` reused as-is either) — it borrows `CameraTimeline`'s already hardware-verified scroll/zoom shape (fixed center needle, the day's tape scrolls underneath, discrete zoom levels via LIVE-056/057) and its three already-fixed bug classes (re-entrant `animateTo`/`jumpTo` infinite recursion, tick-label edge-clipping, echoing a programmatic re-center scroll back as a fresh seek), but keeps `RecordingMapTimeline`'s own two-callback contract (`onSeek` preview-only, `onSeekCommit` firing exactly once per settled gesture) rather than `CameraTimeline`'s per-scroll-frame callback — `CameraTimeline`'s per-frame model is fine for Events' cosmetic browsing but would spam this camera's single-client RTSPS playback listener with a real session reopen on every scroll frame here. Snapshot (LIVE-009) and Record (LIVE-010) are real for the real live feed too: snapshot calls `MediaStreamTrack.captureFrame()` directly on the remote video track (not a screenshot of the video widget, which is unreliable for a texture-backed `RTCVideoView`) and record uses `package:flutter_webrtc`'s `MediaRecorder` against that same track — both still save via the `gal` package into the device photo gallery. Takes `homesController` and looks up the live `Camera` by id on every rebuild (via `ValueListenableBuilder`) instead of using a static navigation snapshot, so settings changed elsewhere (e.g. On-Screen Display's OSD toggles) are reflected immediately without leaving this screen. Also tracks the *phone's* own network type via `connectivity_plus` (LIVE-038/039/040) — a real, live-updating signal, independent of the camera's own Wi-Fi status shown elsewhere on this screen (LIVE-030). Privacy Mode and Video Mode also have quick-access shortcuts here (LIVE-041/042) so those two settings don't require a trip to Camera Settings — both call the real `camera_api` clients directly and apply immediately, unlike the full settings screens' draft-then-Apply convention. **Dev/test only**: this screen's `LiveViewController` reads the Dashboard's DASH-023 Force LAN/WAN override (`DebugTransportOverride`) at construction, and listens for it changing while already open — tearing down and reconnecting fresh under the newly-selected transport, so a tester doesn't need to physically leave the camera's LAN to exercise WAN live view.

## Elements

| Design ID | Element | Type | Notes |
|-----------|---------|------|-------|
| LIVE-001 | Screen title (AppBar) | Text | camera name |
| LIVE-002 | Back button | IconButton (AppBar leading) | returns to the dashboard |
| LIVE-003 | Video area | 16:9 video surface — real `RTCVideoView` (`_HeroVideo`) on the Live tab for a camera with a saved LAN connection, else `_LiveUnavailable` for Live, or the real-clip surface / LIVE-051 for Playback | shows the real WebRTC feed on the Live tab when connected; a "Connecting…"/"Reconnecting…" spinner while `LiveViewController.status` is `connecting`/`reconnecting`; a "Couldn't connect to camera" message + Retry button on `failed`/`stopped`. **Live tab, no saved connection**: `_LiveUnavailable`'s "Camera not connected" message (LIVE-055) — no fake footage; removed the previous bundled dummy sample video 2026-09-07. **Playback tab**: the real currently-open recorded clip (`_PlaybackTabState`/`RecordingsClient`) if one is loaded, else LIVE-051's "No recording available"/loading state — never fake footage either. Pinch-to-zoom (up to 4x, via `_ZoomableVideo`) works on every real surface, both here and in fullscreen, and shows the LIVE-044 zoom-level badge while zoomed. Carries the LIVE-004 badge (top-left), LIVE-007/LIVE-008 overlay buttons (bottom corners). **`camera.isOnline == false` only swaps this for `camera.thumbnailUrl` + the LIVE-028 overlay while the Live tab is active** — Playback is recorded footage, not a live feed, so it keeps showing its own video/state (and LIVE-007/008/047/048/049 stay usable) regardless of the camera's current connectivity, same reasoning as the day picker/timeline/event-nav controls below it, which were never gated on `isOnline` either. (A real bug until 2026-08-28: this whole area — video and LIVE-007/008 — used to go dark on Playback too whenever the camera happened to be offline, even though nothing about Playback actually depends on it.) |
| LIVE-051 | "No recording available" message | `_PlaybackUnavailable` (icon + text over a black background), shown in the LIVE-003 video area | Playback tab only, whenever no real clip is open — either still checking (spinner, no text) or confirmed nothing to show (no saved connection, nothing recorded for the selected day, or the check/download failed). Deliberately never substitutes the bundled dummy asset here, per direct instruction: a camera with no recording shows this message, not fake footage |
| LIVE-004 | Live/Needs Attention/Offline badge | `LiveStatusBadge` (shared widget, `lib/widgets/live_status_badges.dart`), overlaid on the video, top-left (fixed) | shown only when `camera.liveTagOsdEnabled` (toggled from [tags_screen.md](../camera_settings/video_display/tags_screen.md), CAMSET → Video & Display → Tags); driven by `camera.liveStatus` (3-state: `online`/`needsAttention`/`offline`) — green blinking dot + "Live" while healthy and online, amber dot (non-blinking) + "Needs Attention" when online but a health condition is open (see [storage_screen.md](../camera_settings/recording_and_storage/storage_screen.md)'s failure/wear conditions), red dot + "Offline" otherwise; no background box, plain text laid directly over the video |
| LIVE-037 | Audio-recording indicator | `AudioRecordingBadge` (shared widget, `lib/widgets/live_status_badges.dart`), overlaid on the video, top-left (fixed) | shown only when `camera.audioRecordingEnabled` (toggled from [audio_screen.md](../camera_settings/audio_screen.md)'s AUD-012); plain mic icon, no background box; persistent — independent of the LIVE-007 mute/unmute control, which only affects local listening, not what's recorded |
| LIVE-029 | Bitrate badge | `BitrateBadge` (shared widget, `lib/widgets/live_status_badges.dart`), overlaid on the video at `camera.bitrateOsdPosition` via `osdPositioned` | shown only when `camera.bitrateOsdEnabled` (toggled from [tags_screen.md](../camera_settings/video_display/tags_screen.md)); composited by the app, not sent to or rendered by the camera; speed icon + a stub raw-kbps value (e.g. "2048 kbps") — not auto-scaled, unlike Signal Strength's speed reading; no background box |
| LIVE-030 | Signal strength badge | `SignalStrengthBadge` (shared widget, `lib/widgets/live_status_badges.dart`), overlaid on the video at `camera.signalStrengthOsdPosition` via `osdPositioned` | shown only when `camera.signalStrengthOsdEnabled` (toggled from [tags_screen.md](../camera_settings/video_display/tags_screen.md)); bars icon colored red (0–1 bars) / amber (2) / green (3–4) — real, polled every 20s while this screen is open via `NetworkInfoClient.getWifiSignalStrength()` (same LAN call [wifi_config_screen.md](../camera_settings/wifi_config_screen.md) uses) → `barsForRssi` → persisted as `Camera.signalStrength` via `HomesController.updateCamera` (used to always read 0 — nothing ever wrote a real value into that field before). Network speed auto-scaled to KB/s or MB/s via `formatBitrate` — prefers `LiveViewController.measuredBitrateKbps` (a real reading from `RTCPeerConnection.getStats()`'s `inbound-rtp` report, LAN only, null until two samples land after connecting) over the persisted `Camera.networkSpeedKbps` fallback, so this doesn't re-persist a value that changes every couple of seconds into shared state. No numeric bar count shown; composited by the app, not sent to or rendered by the camera; no background box |
| LIVE-005 | Live/Playback tab bar | TabBar | switches the control panel below the video between Live and Playback; tapping is the only way to switch — swiping the panel below is disabled |
| LIVE-007 | Mute/unmute button | IconButton overlaid on the video, bottom-left | toggles real remote-audio-track `enabled` via `LiveViewController.setAudioEnabled` when the Live tab is showing a real connection, and the dummy `VideoPlayerController`'s volume otherwise (both, harmlessly, whenever a real connection exists); disabled (dimmed, `onPressed: null`) while the camera is offline |
| LIVE-008 | Fullscreen toggle | IconButton overlaid on the video, bottom-right | opens the video in a fullscreen landscape page (locks device orientation to landscape while open) — shows whichever surface (real or dummy) was active on the tab that was showing when opened; its own mute (bottom-left) and exit-fullscreen (bottom-right) buttons sit at the bottom corners there too, plus pinch-to-zoom on the dummy surface; disabled while the camera is offline. **Opened from the Playback tab, it also carries its own LIVE-052/053/054 play/pause/skip overlay** (added 2026-09-07 — fullscreen previously had no way to pause or skip a recorded clip at all, found comparing against the reference app's fullscreen, which mirrors its inline controls exactly) |
| LIVE-009 | Snapshot tile | Square card (icon + label) in a single row below the video | Live tab; when connected to a real camera, captures the frame straight from the remote video track (`LiveViewController.captureSnapshot` → `MediaStreamTrack.captureFrame()`); otherwise falls back to a `RepaintBoundary` → PNG screenshot of whatever's on screen (in practice `_LiveUnavailable`'s placeholder, since this tile is disabled while offline anyway — see next). Either way saved via `Gal.putImageBytes`; shows a snackbar on success/failure; disabled (dimmed) while the camera is offline |
| LIVE-010 | Record tile | Square card (icon + label, start/stop toggle) in the row | Live tab; starts a 30-second recording timer, label switches to the live elapsed time (MM:SS) while recording, with a thin progress bar underneath (`_ControlTile.progress`, `recordingElapsed / _recordingLimit`) so the approaching limit is visible at a glance — turns amber for both the bar and the elapsed-time text/icon once 5 seconds remain, not just an abrupt dialog at 0:00 remaining. At the 30-second mark shows LIVE-025 (continue/stop dialog); tapping again while recording stops and saves immediately. **Records whatever is actually on screen, not a track-level capture** (reworked 2026-09-17 — the previous native `MediaRecorder` attached directly to the WebRTC video track only worked on a LAN WebRTC connection and was blocked outright on WAN/RTSP-over-LAN fallback): `_startFrameCaptureTimer` grabs a frame every `1000 ~/ _recordingFps` ms via the same `_captureFrame` helper LIVE-009's snapshot uses (real WebRTC frame capture when the Live tab shows a connected feed, `RepaintBoundary` screenshot otherwise), normalizes it to one fixed resolution (`_recordingTargetSize`, capped at `_recordingMaxDimension` on its long edge) via an offscreen canvas draw, and feeds the raw RGBA bytes to `FlutterQuickVideoEncoder` (hardware MediaCodec/AVFoundation h264, video-only — no audio track) — `Gal.putVideo` on stop. Because it captures on-screen pixels rather than a track, it now works identically regardless of transport (LAN WebRTC, RTSP-over-LAN fallback, or WAN/HLS). Still blocked with a snackbar when there's no camera connection at all ("Add this camera's IP address in Settings to enable recording") or the connection exists but isn't live yet ("Camera isn't live yet — wait for the stream to connect first"). Disabled while the camera is offline |
| LIVE-011 | Talk tile | Square card (icon + label, toggle) in the row | Live tab; real two-way talk, per `packages/camera_api/TWO_WAY_TALK_GUIDE.md`. **Runs on its own dedicated RTSPS connection now (2026-09-15)** — `LiveViewController.startTalk`/`endTalk` open/close an `RtspTalkSession` (`lib/rtsp/rtsp_talk_session.dart`, port 560, `TalkUriClient.getTalkUri()` discovery) completely independent of whatever live view's own connection is doing, replacing the old same-`RTCPeerConnection`-renegotiation mechanism after the camera's firmware removed the `{"talk": true}` flag on `POST /webrtc` entirely (2026-09-11) — that old mechanism was talking to an endpoint the firmware had already stopped honoring, not just a suboptimal design. Because talk no longer shares live view's connection, it now also works on the RTSP-over-LAN live-view fallback, not just when the camera reports WebRTC for a profile, and ending a call no longer tears down or requires reconnecting live view at all. Phone-mic uplink is AAC-LC 8kHz mono via `package:record`'s encoded stream mode, RFC 3640-framed by hand; camera-mic downlink is depacketized the same way live view's own audio already is, then replayed through a local ADTS-over-HTTP loopback (`lib/rtsp/talk_downlink_player.dart`) to a hidden `video_player` — **this downlink playback path is unverified pending hardware** (`TWO_WAY_TALK_GUIDE.md` §5/§8), same honesty flag the guide itself carries. **Hardware capability check, 2026-09-15** (real gap fixed — this was never checked anywhere before): `_loadAudioCapability` fetches `AudioCapabilityClient.getAudioCapability()` once per screen open (LAN-only); tapping Talk on a camera that reports `hasSpeaker == false` shows "This camera doesn't support two-way talk" instead of attempting (and silently failing) a session — `null`/unloaded/WAN-only cameras are treated as "not confirmed unsupported" (same `!= false` convention LIVE-018/019/020's siren/spotlight/warning capability gates already use), so a mid-onboarding or WAN-only camera isn't wrongly told it lacks hardware nobody's actually checked. Also requires the Live tab's connection to already be `connected` and on LAN transport to start (talk has no WAN leg) — shows a snackbar ("Connect to the camera to talk", or "Talk isn't available on this connection" on WAN) and does nothing otherwise. Icon/label reflect `LiveViewController.talkStatus != TalkStatus.idle`, not local state. Status/controls while active show inline as the TALK-001–005 bar (below), not a separate screen — the video keeps playing throughout, unaffected by talk starting/ending. Disabled while the camera is offline |
| LIVE-012 | Recording map | `ScrollableRecordingTimeline` (`lib/widgets/scrollable_recording_timeline.dart`) — owns its own `GlassCard`, plain `GestureDetector`, no `ScrollController` | Playback tab only. **Rebuilt from scratch 2026-09-08** after the previous `ScrollController`-based version (fixed center needle, the day's tape scrolling underneath via `SingleChildScrollView`) kept producing real, recurring time-sync drift between the displayed time/needle and the actual playing video despite several targeted fixes — each closed one specific race without removing the underlying *class* of bug: a `ScrollController` carries its own pixel-offset state independent of the epoch it's meant to represent, and every translation between the two was one more chance for them to disagree, especially around zoom-level changes. This version has no scroll position at all — the visible window's center is always exactly `cursorEpochSeconds` (or, mid-drag, the drag's own live value), and every tick/recording-segment/event-marker position is computed directly from `epoch − halfWindowSeconds` on every build, so the needle (always drawn at the exact horizontal center) and the header time label read the literal same value by construction — there is no second, independently-updated value left to disagree with it. Dragging is a plain `GestureDetector.onHorizontalDragUpdate` converting pixel delta straight to a time delta; tapping jumps directly to that point. Zoom levels (LIVE-056/057) are "Hour" down to "1 min" — **"Day" was removed** (real user reports traced the drift specifically to the widest level, and a full 24h squeezed into one screen width was unreadable anyway once the timeline started always spanning the full day). Preview-only `onSeek` fires continuously while dragging; `onSeekCommit` fires exactly once when the drag actually ends (snapping into the nearest recorded clip if it ends in a gap) — this is what re-binds the real RTSP session (see LIVE-036); a drag never spams the camera with concurrent session opens, since nothing reopens per drag-update frame. Tick labels that wouldn't fit fully within the visible width are skipped rather than showing a clipped fragment. Recorded segments fill in `kRecordedBandColor` (`lib/widgets/camera_timeline.dart`'s own sky-blue constant, "Recorded" in Events' own legend too), not the theme's `colorScheme.primary`, so "recorded" reads as an unambiguous, consistent blue regardless of the app's theme seed color, matching Events' own timebar. Event markers (`RecordingClipSpan.hasEvent`, the same `RecordingClip.trigger` signal LIVE-013/020 already used) render as colored hatch blocks with an icon, the same visual language `CameraTimeline`'s own `TimelineMarker` gives Events' motion/person/alert markers — still spans the full calendar day (`_dayStartEpoch`/`_dayEndEpoch`) as its outer bound for dragging/tapping, a stable 24h reference the same way mainstream CCTV apps anchor theirs |
| LIVE-056 | Zoom out button | IconButton, top-right of LIVE-012 next to LIVE-057 | Steps to the previous (wider-view) zoom level; disabled at the widest ("Day") level. Recenters on the current cursor position after stepping |
| LIVE-057 | Zoom in button | IconButton, right of LIVE-056 | Steps to the next (narrower-view, more precise) zoom level; disabled at the narrowest ("1 min") level. Recenters on the current cursor position after stepping |
| LIVE-013 | Prev Event button | TextButton (chevron + label) | Playback tab; jumps the cursor to the previous clip that has a `RecordingClip.trigger` (a matching alert in the camera's recent history — see that field's own doc) before the current position — snaps to the nearest recorded clip first if that timestamp itself falls in a gap (not visually marked on LIVE-012 since that timeline shows only the recording band) |
| ~~LIVE-014~~ | ~~Playback time display~~ | — | Retired 2026-09-08 — LIVE-012's own recreated header now shows the time-of-day readout internally (matching `CameraTimeline`'s layout), making this separate outer text redundant |
| LIVE-015 | Playback snapshot button | IconButton | Playback tab — **real capture**, shared with LIVE-009's `_takeSnapshot`: since `_captureFrame`'s real-WebRTC-track path only applies on the Live tab, calling it here always takes its `RenderRepaintBoundary` screenshot fallback of the shared video area, i.e. a real still of whatever's currently on screen, saved via `Gal.putImageBytes` |
| LIVE-016 | Save clip button | IconButton.filled ("Save clip"), replaced by a small `CircularProgressIndicator` while saving | Playback tab — **real save-to-gallery**: downloads the *whole* currently-open clip via `RecordingsClient.downloadClip` (the one remaining legitimate use of the HTTP download path — RTSP streaming produces no local file to trim) and saves it via `Gal.putVideo`. Enabled only while a real clip is open (`_currentClip != null`). No clip-range trim handles any more (see removed LIVE-023/LIVE-024 below) — those existed to trim a downloaded temp file, which RTSP streaming no longer produces; saving the whole clip is the direct equivalent the sibling `nuraeye-rt` app's own clip screens offer |
| LIVE-038 | Connection-type indicator | Icon + text, AppBar action (before LIVE-017) | reflects the *phone's* network — not the camera's Wi-Fi signal (that's LIVE-030) — via the `connectivity_plus` package; Wi-Fi icon + "Wi-Fi" or cellular-bars icon + "Mobile data", updating live if the phone's network changes mid-session without interrupting playback; hidden entirely when connectivity is `none`. Appends the real measured throughput (`LiveViewController.measuredBitrateKbps`, the same `RTCPeerConnection.getStats()` reading LIVE-030 uses, via `formatBitrate`) once available — e.g. "Wi-Fi · 1.2 MB/s" — showing the actual rate currently arriving on whichever network the phone is on, not just the connection type; omitted (falls back to the plain type-only text) before the first two stats samples land or on WAN, where no equivalent measurement exists |
| LIVE-039 | Cellular-data confirmation dialog | AlertDialog, shown once on screen entry | shown only if the camera is online **and** the phone is on cellular (not Wi-Fi) when this screen opens; "You're on mobile data — streaming video will use your mobile data. Continue?"; Cancel pops back to Dashboard without starting playback, Continue proceeds normally |
| LIVE-040 | Cellular-data reminder | `MaterialBanner` (persistent, dismissible), not a periodic snackbar | Shown once when the phone is (or becomes) on cellular while this screen stays open, and stays visible — not re-shown on a timer — until the user taps Dismiss or connectivity returns to Wi-Fi (whichever first). Replaced a recurring "Still watching on mobile data" snackbar every 5 minutes 2026-09-07: a snackbar that vanishes after a few seconds is easy to miss, and re-showing it periodically didn't fix that, just repeated it |
| LIVE-058 | Stream Quality chip | `ActionChip`, AppBar action (before LIVE-017) | transport-aware (`_currentStreamQualityChipLabel`, 2026-09-15): on LAN with Auto selected, "Auto" alone or "Auto-H"/"Auto-M"/"Auto-L" once resolved against `LiveViewController.measuredBitrateKbps` (the same live reading LIVE-030/LIVE-038 already use — ≥2000 kbps→High, ≥800→Medium, else Low); with a manual pick, the camera's own real profile name (`MediaProfile.name`, capitalized — e.g. "High"), cross-referencing `Camera.preferredLanProfileToken`/`LiveViewController.currentProfileToken` against `LiveViewController.lanProfiles`, not a generic bucket label. On WAN, reads `LiveViewController.wanQuality`/`wanAutoQuality` instead (`Camera.streamQuality` is a LAN-only preference and wouldn't reflect what's actually playing) — "Auto-H"/"Auto-M"/"Auto-L" (the tier the ladder is actually on, not a bitrate guess — WAN has no bitrate telemetry) or the manual level's name. Tapping opens LIVE-059 (LAN) or the WAN variant below |
| LIVE-059 | Stream Quality bottom sheet | `showModalBottomSheet`, one `RadioGroup`/`RadioListTile` per option | **LAN branch, redesigned 2026-09-15**: manual options are no longer a hardcoded High/Medium/Low trio — `MediaProfile`'s own doc comment (`onvif_video_encoder_client.dart`) is explicit that a call site must "never hardcode 3 streams... always read this list and its length," which the old fixed-3-bucket-by-sorted-index mapping violated (a camera with only 2 profiles silently collapsed "Medium" and "Low" onto the identical token). Now renders LIVE-060 (Auto) plus one tile per profile `LiveViewController.loadLanProfiles()` (`OnvifVideoEncoderClient.getProfiles()`) actually reports for this camera, sorted by resolution descending (LIVE-068+, however many that turns out to be), each labeled with the camera's own profile name and its real resolution. Auto requests `kMobileOnlyStreamProfileToken` via `setPreferredProfile` and calls `LiveViewController.setAutoQualityLadder(true)`, re-enabling automatic step-down/step-up on sustained LAN trouble/recovery (STREAMING_GUIDE.md §2.1 — previously disabled outright on this screen after a past confusing-background-change bug; re-enabled now but gated strictly behind an explicit Auto selection, and every automatic change fires a SnackBar via `LiveViewController.autoQualityChangeMessage` so it's never silent). A manual pick calls `setAutoQualityLadder(false)`, persists the exact token to `Camera.preferredLanProfileToken`, and pins to it via `setPreferredProfile`. **WAN branch** (`FR-CF-154`, updated 2026-09-15): when `LiveViewController.transport == LiveViewTransport.wan`, opens the WAN variant of this sheet instead (LIVE-064/065/066/067) — fixed named tiers, not a dynamic profile list (WAN's KVS tiers carry no discoverable resolution metadata the way ONVIF profiles do); Auto is back, backed by `LiveViewController.setWanAutoQuality`/`_pollWanStall`'s stall-detection stepping (no bitrate telemetry exists over HLS, STREAMING_GUIDE.md §8, so stall/rebuffer is the adaptation signal instead); a manual pick calls `setWanQuality`, tearing down the current viewer lease (`stopCloudStreaming`) and reconnecting directly over WAN on the new tier — same real, separately-billed-KVS-stream cost an automatic step incurs too. WAN quality (auto or manual) is session-only (resets to Medium, auto off, on a fresh connect), unlike the LAN branch's persisted `Camera.streamQuality`/`Camera.preferredLanProfileToken` |
| LIVE-060 | Stream Quality: Auto (LAN) | `RadioListTile<String>`, LAN variant of LIVE-059 | "Adjusts automatically based on your connection" — see LIVE-059's LAN branch |
| LIVE-064 | WAN Stream Quality: High | `RadioListTile<CameraStreamQuality>`, WAN variant of LIVE-059 | selects `StreamQuality.high` — "Best quality, uses the most data" |
| LIVE-065 | WAN Stream Quality: Medium | `RadioListTile<CameraStreamQuality>`, WAN variant of LIVE-059 | selects `StreamQuality.medium` (the default every fresh WAN connection starts on) — "Balanced quality and data usage" |
| LIVE-066 | WAN Stream Quality: Low | `RadioListTile<CameraStreamQuality>`, WAN variant of LIVE-059 | selects `StreamQuality.low` — "Lowest data usage, reduced quality" |
| LIVE-067 | WAN Stream Quality: Auto | `RadioListTile<CameraStreamQuality>`, WAN variant of LIVE-059 | **Added 2026-09-15.** Calls `LiveViewController.setWanAutoQuality(true)` — "Adjusts automatically based on your connection"; steps `wanQuality` down a tier after `_wanStallThreshold` (3) consecutive stalled `_pollWanStall` ticks (3s interval, ~9s), back up after `_wanHealthyPollThreshold` (10) consecutive healthy ticks (~30s) — deliberately slower to step up than down, same asymmetric reasoning as the LAN ladder |
| LIVE-068+ | Stream Quality: per-profile (LAN) | `RadioListTile<String>`, LAN variant of LIVE-059, one per position (068, 069, 070, …) | **Added 2026-09-15.** One tile per `LiveViewController.lanProfiles` entry, sorted by resolution descending — LIVE-068 is the camera's highest-resolution profile, LIVE-069 the next, and so on; a camera with fewer profiles simply doesn't render the higher-numbered tiles that session (same "hidden when not applicable" treatment LIVE-018 already documents for a missing capability). Title is the profile's own `MediaProfile.name` capitalized (e.g. "High"); subtitle is its real resolution (e.g. "2560×1440") straight from `GetProfiles`, not an app-side guess. Selecting one persists its exact token to `Camera.preferredLanProfileToken` |
| LIVE-017 | Settings button | IconButton (AppBar action) | navigates to [camera_settings_screen.md](../camera_settings/camera_settings_screen.md) |
| LIVE-018 | Spotlight tile | Square card (icon + label, toggle) in the row | Live tab — triggers/stops the camera's physical spotlight via `DeterrenceClient.activateDeterrence`/`.deactivateDeterrence('spotlight')` (real, when connected, retried over `WanDeterrenceClient` if the LAN call fails and `connection.thingName` is known) or `simulateCameraSave` (local-only fallback), applied immediately with no draft/Apply step; shows an error snackbar and leaves the tile in its prior on/off state on failure. No duration is sent — the camera applies its own configured auto-stop duration. Momentary hardware state only, not persisted to `HomesController`. Initial on/off state is fetched from `DeterrenceClient.getDeterrenceStatus()` on screen open (`_loadShortcutState`, LAN-only, best-effort) rather than assumed off, so an auto-triggered Response Action (see [alert_settings_screen.md](../alerts/alert_settings_screen.md)) or another client's manual trigger already show up correctly the moment this screen opens. **Hidden entirely (not just disabled) when `Camera.spotlightCapable == false`** — the onboarding `CapabilitiesClient.getCapabilities()` snapshot (`camera_sync.dart`); `null` (not yet synced) still shows it, only a definitive `false` hides it, same treatment `wanLiveViewCapable` gets elsewhere. When shown, together LIVE-009/LIVE-010/LIVE-011/LIVE-018 form a single equal-width `Row` of `GlassCard` tiles (icon above a text label) directly below the video/tab bar — not a pill toolbar, not a 2-column grid; the row simply has one fewer tile when Spotlight is hidden. Disabled while the camera is offline or a request for this tile is already in flight |
| LIVE-041 | Privacy shortcut tile | Square card (icon + label) in a second row below LIVE-009/010/011/018 | Live tab. Real `camera_api` call (`PrivacyModeClient.setPrivacyMode`, same client [privacy_mode_screen.md](../camera_settings/video_display/privacy_mode_screen.md) uses, retried via `WanPrivacyModeClient.setPrivacyMode` if the LAN call fails and `connection.thingName` is known, per `.claude/rules/mobile-app-screen-conventions.md`'s LAN/WAN convention), applied immediately on tap — no draft/Apply step, matching LIVE-007/LIVE-008's instant-apply pattern rather than the settings-screen Apply convention. Only toggles Off↔Full; if the camera is currently in Zone mode, tapping turns it Off instead of jumping to Full (Zone's own mask configuration is untouched, still only editable from the full Privacy Mode screen). Label/icon reflect the current `camera.privacyMode` from the cached `Camera` model (Off = eye icon, Full = eye-off icon + `AppColors.offline` tint, Zone = square icon) — refreshed from a real `PrivacyModeClient.getPrivacyMode()` read on screen open (`_loadShortcutState`, LAN-only, best-effort), so it no longer only reflects whatever this app itself last set. Disabled while the camera is offline or a request from this tile is already in flight |
| LIVE-042 | Video Mode shortcut tile | Square card (icon + label) in the same second row | Live tab. Real `camera_api` call (`OnvifImagingClient.setImagingSettings(irCutFilterMode: ...)`, same underlying setting as [video_mode_screen.md](../camera_settings/video_display/video_mode_screen.md), retried via `WanImagingClient.setDayNightMode` if the LAN call fails and `connection.thingName` is known), applied immediately on tap. Cycles Day → Auto → Night → Day on each tap (sun/auto/moon icon, label shows the current mode) — unlike VIDMODE-004, doesn't gate the offered modes on the camera's reported `IrCutFilter` capability list, so a camera lacking one of the three surfaces that as a failed Set rather than hiding the option here. Current mode refreshed from a real `OnvifImagingClient.getImagingSettings()` read on screen open (`_loadShortcutState`, same as LIVE-041). Disabled while the camera is offline or a request from this tile is already in flight |
| LIVE-044 | Zoom-level badge | Text overlaid on the video, top-right (fixed), inside `_ZoomableVideo` | shows the current pinch-zoom scale as e.g. "Digital Zoom: 1.0x"/"Digital Zoom: 2.3x" (from `TransformationController.value.getMaxScaleOnAxis()`); appears while actively pinching and fades out ~1s after the gesture ends; present on the real (`RTCVideoView`) Live surface and the real-clip Playback surface (`video_player`), and in fullscreen — anywhere pinch-to-zoom (LIVE-003) works |
| LIVE-045 | Siren tile | Square card (icon + label, toggle) in a row below LIVE-041/042, shown when present alongside LIVE-046 | Live tab — triggers/stops the camera's physical siren via `DeterrenceClient.activateDeterrence`/`.deactivateDeterrence('siren')` (real, when connected, retried over `WanDeterrenceClient` if the LAN call fails and `connection.thingName` is known) or `simulateCameraSave` (local-only fallback), same instant-apply/no-duration/momentary-state pattern as LIVE-018, including the same `getDeterrenceStatus()`-on-open refresh. **Turning it on asks for confirmation first** — LIVE-050's dialog ("Sound siren?" / Cancel / Sound siren), since an accidental tap triggers a real, loud, disruptive alarm; turning it back off is instant, no confirmation. **Hidden entirely when `Camera.sirenCapable == false`** — same `null`-still-shows treatment as LIVE-018. Disabled while the camera is offline or a request for this tile is already in flight |
| LIVE-050 | "Sound siren?" confirmation dialog | AlertDialog | Shown only when tapping LIVE-045 to turn the siren **on** (not off). "This will trigger a loud audible alarm on the camera." / Cancel / Sound siren. Cancel (or dismissing) leaves the siren untouched, no request sent |
| LIVE-052 | Back 10 seconds button (fullscreen) | `_VideoOverlayButton`, in `_FullscreenVideo`'s own tap-to-reveal overlay centered on the video | Fullscreen equivalent of LIVE-047, added 2026-09-07 — fullscreen previously had only mute/exit, no way to pause or skip a recorded clip. Shown only when fullscreen was opened from the Playback tab. Same tap-anywhere-on-video-to-reveal, 4s-auto-hide behavior as LIVE-047/048/049, tracked independently in `_FullscreenVideoState` (its own overlay-visibility state, separate from the inline screen's) |
| LIVE-053 | Play/Pause button (fullscreen) | `_PlaybackPlayPauseButton`, center of the row with LIVE-052/LIVE-054 | Fullscreen equivalent of LIVE-048 — same `ValueListenableBuilder<VideoPlayerValue>`-driven icon/tooltip, calling back into `_CameraLiveScreenState._togglePlaybackPlayPause` (the clip controller and `_PlaybackTabState` both live in the still-mounted screen underneath; fullscreen is a `Navigator.push`, not a replacement) |
| LIVE-054 | Forward 10 seconds button (fullscreen) | `_VideoOverlayButton`, right of LIVE-053 in the same overlay row | Fullscreen equivalent of LIVE-049, calling back into `_CameraLiveScreenState._skipPlaybackVideo` the same way LIVE-053 calls back for play/pause |
| LIVE-046 | Warning tile | Square card (icon + label, toggle) in the same row as LIVE-045 | Live tab — triggers/stops the camera's warning light/sound via `DeterrenceClient.activateDeterrence`/`.deactivateDeterrence('warning')`/`WanDeterrenceClient` fallback, same pattern as LIVE-018/LIVE-045, including the same `getDeterrenceStatus()`-on-open refresh. **Hidden entirely when `Camera.warningCapable == false`**. The LIVE-045/LIVE-046 row itself only appears at all when at least one of the two is shown; if only one is capable, that tile alone fills the row. Disabled while the camera is offline or a request for this tile is already in flight |
| — | `_loadShortcutState()` | Not a UI element — screen-open behavior, `initState`, LAN-only, best-effort | Fetches `PrivacyModeClient.getPrivacyMode()`, `OnvifImagingClient.getImagingSettings()`, and `DeterrenceClient.getDeterrenceStatus()` once when this screen opens (alongside the existing `_loadRealBitrate`/`_pollSignalStrength` fetches), so LIVE-041/042/018/045/046 reflect the camera's actual current state rather than only whatever this app itself last set. A failed fetch leaves whatever the tiles already had — doesn't block the screen or show an error |
| LIVE-043 | AI Mode tile | Square card (icon + label), full-width, row below LIVE-041/042 and LIVE-045/046 | Live tab. Captures the current frame (same `_captureFrame` helper LIVE-009's snapshot uses) and opens [ai_mode_screen.md](ai_mode_screen.md) via `Navigator.of(context, rootNavigator: true).push(...)` — same escape-the-bottom-nav-shell technique as LIVE-008's fullscreen video, not a nested `GoRoute` — a look-and-feel prototype (no real AI backend wired up yet) for circling an object in the frame and asking about it. Disabled while the camera is offline |
| LIVE-019 | Recordings day selector | Button opening a dropdown (PopupMenu) of day entries — or a plain (non-tappable) "No recordings in the last 90 days" label when there are none, or a loading bar while checking | Playback tab — button shows the selected day (e.g. "Today · 12:00 AM – 11:59 PM"). **Only lists dates that actually have footage** (2026-09-07 fix — previously always listed the last 7 calendar days regardless of whether anything was recorded): `_loadDatesWithRecordings` scans the last 90 days via one bounded `RecordingsClient.getRecordings` call, buckets each clip's start time into its **local** calendar date, and lists only those dates, newest first, same approach as the sibling `nuraeye-rt` app's `RecordingTimelineScreen._loadDatesWithRecordings`. Picking a date calls a second, day-bounded `getRecordings(start, end)` (local midnight to local midnight, `.millisecondsSinceEpoch` on a local `DateTime`) and opens the first real clip found |
| LIVE-020 | Next Event button | TextButton (label + chevron) | Playback tab; jumps the cursor to the next clip that has a `RecordingClip.trigger` after the current position (not visually marked on LIVE-012) |
| LIVE-036 | Recording-availability band | Filled segments `ScrollableRecordingTimeline` paints for each clip | Playback tab; shows where real footage exists for the selected day — one filled segment per `RecordingClip` returned by `RecordingsClient.getRecordings`. **Gaps render fully blank** (changed 2026-09-07 per direct user request — previously a faint filled "track" pill ran the full width, gaps included, which made it harder to see at a glance where footage actually exists; the track is now drawn as a thin outline only, filled segments the only solid color on the bar). **Real RTSP streaming, not download-then-play** (`_PlaybackTabState._openClip`, rebuilt entirely 2026-09-07): resolves the clip to a playable `rtsp(s)://` URI via `OnvifReplayControlClient.getReplayUri`, opens an `RtspRemuxProxy` (`lib/rtsp/`, ported near-verbatim from the sibling `nuraeye-rt` app's own hardware-verified RTSP client/fMP4-muxer/local-loopback-proxy stack) against it, and points a `VideoPlayerController.networkUrl` at the proxy's local HTTP loopback — no `downloadClip`/temp-file step for playback any more (that path is now used only by LIVE-016's Save). A 1-second position timer tracks the camera's own server-side-authoritative position and the player's own decode position, and reaching a clip's real end (`RtspRemuxProxy.isSessionEnded` — this camera's RTSPS playback listener has no end-of-stream signal, just silence) auto-advances to the next clip that day, or stays on the last frame if it was the last one. Landing in a gap — via LIVE-013/020/047/049, or a cursor drag that *ends* in one — jumps directly to the nearest recorded clip. No connection, nothing recorded, or a session-open failure all show LIVE-051 instead of any video. **Seeking always means a fresh RTSP session** (`DESCRIBE`/`SETUP`/`PLAY` with a `Range: clock=...` header) — this camera's playback listener has no seek-in-place or `PAUSE` method at all, so the Playback tab becoming inactive (switching to Live, or backgrounding the app) fully tears the session down (remembering the cursor position) rather than just pausing local rendering, and reopens fresh at that position on return — same reasoning `_CameraLiveScreenState._syncLiveConnectionForActiveTab` already applies to the Live tab's own WebRTC session while Playback is active. `ScrollableRecordingTimeline.onSeek` (live scroll preview) never itself opens a session — only `onSeekCommit` (a settled scroll/zoom gesture) does, once per gesture, which is what actually keeps scrolling from spamming the camera with concurrent session opens; `_pendingClipId`/`_openClipRequestId` exist to make a *newer* seek's `_openClip` call always supersede whatever older one was still in flight (real bug, fixed 2026-09-07: an earlier version skipped calling `_openClip` at all whenever the target clip matched one already opening, which silently dropped a second seek to a different offset within the same clip — see `_jumpTo`'s own doc). **A real clip opening successfully is also treated as a reachability signal** (`_CameraLiveScreenState._syncCameraOnlineFromPlayback`, added 2026-09-07) — since `_syncLiveConnectionForActiveTab` deliberately stops Live's own WebRTC connection while Playback is active, nothing on the Live side would otherwise notice the camera recovering from an earlier outage while sitting on this tab; a successfully-opened clip writes `camera.isOnline: true` the same way Live's own successful connection does |
| LIVE-047 | Back 10 seconds button | `_VideoOverlayButton` (circular, translucent black), in a tap-to-reveal overlay centered on the video | Playback tab only (works regardless of the camera's live online/offline status — Playback is recorded footage). Skips the cursor back 10 real seconds (`_PlaybackTabState.skipSeconds`, changed from a 10-*minute* jump 2026-09-07 per direct user request — too coarse for scrubbing near an event), jumping to the nearest recorded clip if that lands in a gap — same as LIVE-036's drag-into-gap behavior. Tapping it resets the 4s auto-hide countdown (see LIVE-048) |
| LIVE-048 | Play/Pause button | `_PlaybackPlayPauseButton` wrapping `_VideoOverlayButton`, center of the row with LIVE-047/LIVE-049 | Playback tab only (works regardless of the camera's live online/offline status); disabled while no real clip is open. Toggles the currently-open clip's play/pause state — icon and tooltip are reactive via `ValueListenableBuilder<VideoPlayerValue>` on the clip controller itself (fixed 2026-09-07 — previously read `.value.isPlaying` once at build time and only looked correct because a once-a-second position timer happened to force a rebuild every tick; playback pausing/ending on its own between ticks could leave the icon stale). **The whole LIVE-047/048/049 row is a tap-to-reveal overlay on the video itself** (`GestureDetector` + `AnimatedOpacity`, `Positioned.fill` over the video, inserted before LIVE-007/008 in the Stack so those two stay independently tappable): hidden by default, a tap anywhere on the video (not just this row) toggles it, and it auto-hides again after 4 seconds of no interaction with it (tapping any of the three buttons restarts that countdown; tapping the video again while visible hides it immediately). Only rendered at all while the Playback tab is the active tab — switching to Live resets it back to hidden so returning to Playback later starts fresh, not mid-countdown. **LIVE-052/053/054 are the same row's fullscreen equivalent** — see LIVE-008 |
| LIVE-049 | Forward 10 seconds button | `_VideoOverlayButton`, right of LIVE-048 in the same overlay row | Playback tab only (works regardless of the camera's live online/offline status). Skips the cursor forward 10 real seconds, jumping to the nearest recorded clip if that lands in a gap. Same auto-hide-reset behavior as LIVE-047 |
| LIVE-025 | Continue-recording dialog | AlertDialog | shown when LIVE-010's recording hits the 30-second limit; body counts down from 5 seconds; auto-dismisses as "Stop" if neither button is tapped in time |
| LIVE-026 | Stop button (in LIVE-025) | TextButton | ends the recording now and saves it |
| LIVE-027 | Continue button (in LIVE-025) | FilledButton | keeps recording (the frame-capture timer never stopped — only the 30-second check-in ticker resets), LIVE-025 will reappear after another 30 seconds |
| LIVE-028 | Offline overlay | Dark overlay over the camera's last-known thumbnail | shown when `!camera.isOnline` **and the Live tab is active**; icon + "Camera Offline" + "Last seen …" (if `camera.lastSeen` is set) over `camera.thumbnailUrl` — same visual language as the offline overlay on the Dashboard's camera tiles. Never shown on the Playback tab, which stays fully usable regardless of the camera's current online/offline status — see LIVE-003's note |
| LIVE-031 | Recording-in-progress leave dialog | AlertDialog | shown when the user tries to leave (back gesture or bottom-nav tap, via the shared `LeaveGuard` widget, `lib/widgets/navigation_leave_guard.dart`) while LIVE-010 is actively recording |
| LIVE-032 | Cancel button (in LIVE-031) | TextButton | stays on the screen, recording keeps running |
| LIVE-033 | Stop & Leave button (in LIVE-031) | FilledButton | stops and saves the recording (`_stopRecording(save: true)`), then leaves |
| TALK-001 | Talk status bar | Semi-transparent bar (`_TalkStatusBar`) overlaid at the bottom edge of the video, shown whenever `talkStatus != TalkStatus.idle` — **not a separate screen**; the video keeps playing behind/above it throughout the call (changed 2026-08-14 from an earlier full-screen call panel, which hid the video — the opposite of what you want while talking to someone on camera) | started by LIVE-011 via `_toggleTalk`/`LiveViewController.startTalk()`; shifts LIVE-007/LIVE-008 up (`_cornerButtonBottomInset`) so they don't sit underneath it |
| TALK-002 | Status icon | Icon, small, left-aligned in the bar | reflects `TalkStatus`: phone icon (connecting), mic icon in `AppColors.cyan` (talking), person-off icon in `AppColors.offline` (busy), error icon in `AppColors.offline` (error) |
| TALK-003 | Status text | Text, fills remaining bar width | "Calling…" / "Talking" / "Camera's busy — someone else is talking" (the `HTTP 409` case, deliberately worded distinctly from a hard error per `TWO_WAY_TALK_GUIDE.md` §5.1) / the error message / "Ending…" |
| TALK-004 | Speakerphone toggle | IconButton, right side of the bar, only shown while `TalkStatus.talking` | route-to-loudspeaker, **defaulted on** (`LiveViewController.speakerphoneOn`) per §5.2 — a quiet earpiece-routed default was a real "too quiet" complaint this app hit before. Calls `flutter_webrtc`'s `Helper.setSpeakerphoneOn`. Distinct from LIVE-007's mute, which affects plain live-view playback, not an active call |
| TALK-005 | End button | IconButton (red/`AppColors.offline`), right side of the bar | calls `_toggleTalk`, which ends the talk session via `LiveViewController.endTalk()`. **Ending a `talking` session tears down the entire WebRTC connection, not just the talk leg** (`TWO_WAY_TALK_GUIDE.md` §3 — `POST /webrtc/stop` is defined that way), so live view reconnects fresh (spinner) right after: this is expected, not a bug. Ending from a `busy`/`error` state (talk was never actually granted) only resets local state — live view was never touched |

Playback clip handles (LIVE-023/LIVE-024) are not guarded on leave — they're low-stakes scratch state and are simply lost if the user navigates away before extracting.

LIVE-004/LIVE-037/LIVE-029/LIVE-030 are computed together by `_buildOsdTags` (private helper on `_CameraLiveScreenState`): if two or more of these four tags land on the same corner, `osdStackIndices` (`lib/widgets/live_status_badges.dart`) lines them up side by side, growing inward from that corner's horizontal edge — in Live tag → audio-recording indicator → Bitrate → Signal Strength priority order — instead of letting them render on top of each other.
