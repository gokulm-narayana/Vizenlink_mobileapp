# <ScreenName>

- **Dart file:** `lib/screens/<path>/<screen_name>.dart`
- **Route:** `/<route-path>`
- **Purpose:** one-sentence description of what this screen is for.

## Elements

| Design ID | Element | Type | Notes |
|-----------|---------|------|-------|
| SCR-001   | App bar title | Text | |
| SCR-002   | Primary action button | ElevatedButton | |

<!--
Design ID convention: <SCREEN-PREFIX>-<3-digit-sequence>, e.g. LOGIN-001, LOGIN-002.
Pick a short screen prefix (2-6 uppercase letters) unique across docs/screens/.
Every interactive or visually-distinct element (buttons, fields, labels, images,
list items, icons) gets one ID. IDs are stable once assigned — don't renumber
when elements are added/removed; just add new IDs at the end or retire old ones.
Reference the ID in code via a comment or a `Key('SCR-001')` on the widget so
design <-> code stays traceable.
-->
