# client_code_inbox

Drop your senior's shared Dart files here as-is (keep original filenames).

- `client-code-docs` and `integrate-client-code` look here first for input files.
- Files here are reference/staging only — not part of the app's build. Once a file is documented and integrated, its logic lives in the wired-up screen(s) under `lib/`; the original stays here for reference unless you choose to delete it.
