name: camera_api
description: >
  Pure-Dart, UI-independent camera-access API (LAN ONVIF/NuraEye, WAN AWS IoT/KVS).
  No package:flutter dependency by design — see design/stages/mobile-app-android-1-video-image-pipeline/DESIGN.md.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  http: ^1.2.0
  xml: ^6.5.0
  crypto: ^3.0.3
  pointycastle: ^3.9.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
